#![allow(dead_code, unused_variables)] // TODO(mhs): keep it in early development
#![allow(clippy::needless_range_loop)]
#![feature(f16, int_roundings, variant_count)]

mod machine;
pub use machine::*;
